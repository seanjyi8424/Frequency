import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context';

const Live = () => {
    return(
        <SafeAreaView>
            <Text>
                Live Page
            </Text>
        </SafeAreaView>
    );
}

export default Live